import React from "react";
import AddComment from "../AddComment";
const NewComment = ({ submitDataHandler }) => {
  return (
    <div>
      <AddComment submitDataHandler />
    </div>
  );
};

export default NewComment;
